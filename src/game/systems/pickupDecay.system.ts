/**
 * Pickup Decay System — §1.10.11
 * Apply decay to pickups on the map.
 */
import type { World } from '../ecs/world'

export function pickupDecaySystem(_world: World): void {
  // TODO Phase 6 (resource system)
}
