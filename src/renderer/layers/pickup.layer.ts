/**
 * Pickup Layer — Tech.md §6.2
 * Resource pickup sprites and decay indicators.
 * TODO: Phase 6 renderer implementation
 */

import type { Container } from 'pixi.js'
import type { World } from '@game/ecs/world'

export function updatePickupLayer(_container: Container, _world: World, _alpha: number): void {
  // TODO: render pickups
}
